# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build exp-monorepo`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t exp-monorepo`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system exp-monorepo && \
          adduser --system -G exp-monorepo exp-monorepo

COPY dist/apps/exp-monorepo exp-monorepo/
RUN chown -R exp-monorepo:exp-monorepo .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix exp-monorepo --omit=dev -f install

CMD [ "node", "exp-monorepo" ]
